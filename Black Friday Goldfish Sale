<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>金魚 Black Friday 狂歡祭 - 789幸運抽</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- React & ReactDOM -->
    <script src="https://unpkg.com/react@18/umd/react.development.js" crossorigin></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js" crossorigin></script>
    <!-- Babel for JSX -->
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC:wght@400;700;900&display=swap" rel="stylesheet">
    
    <style>
        body {
            font-family: 'Noto Sans TC', sans-serif;
            background-color: #050505;
            overflow-x: hidden;
        }
        
        /* Custom Animations */
        @keyframes swim {
            0% { transform: translateX(-100%) translateY(0) rotate(0deg); opacity: 0; }
            10% { opacity: 1; }
            50% { transform: translateX(50vw) translateY(-20px) rotate(5deg); }
            100% { transform: translateX(100vw) translateY(0) rotate(0deg); opacity: 0; }
        }

        @keyframes neonPulse {
            0%, 100% { text-shadow: 0 0 10px #ff00de, 0 0 20px #ff00de, 0 0 40px #ff00de; }
            50% { text-shadow: 0 0 5px #00ffff, 0 0 10px #00ffff, 0 0 20px #00ffff; }
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .goldfish-bg {
            position: fixed;
            top: 20%;
            left: -100px;
            width: 150px;
            opacity: 0.6;
            animation: swim 15s linear infinite;
            pointer-events: none;
            z-index: 0;
            filter: hue-rotate(45deg) drop-shadow(0 0 10px orange);
        }
        .goldfish-bg:nth-child(2) {
            top: 60%;
            animation-duration: 20s;
            animation-delay: 2s;
            width: 200px;
            filter: hue-rotate(180deg) drop-shadow(0 0 10px cyan);
        }
        .goldfish-bg:nth-child(3) {
            top: 10%;
            animation-duration: 12s;
            animation-delay: 7s;
            width: 120px;
            filter: hue-rotate(90deg) drop-shadow(0 0 10px magenta);
        }

        .neon-text {
            animation: neonPulse 3s infinite alternate;
        }

        .glass-panel {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .gradient-text {
            background: linear-gradient(to right, #ff00cc, #3333ff, #00ffff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
    </style>
</head>
<body class="text-white min-h-screen relative">
    
    <!-- Background Decor -->
    <img src="https://cdn-icons-png.flaticon.com/512/2829/2829828.png" class="goldfish-bg" alt="Goldfish">
    <img src="https://cdn-icons-png.flaticon.com/512/2829/2829828.png" class="goldfish-bg" alt="Goldfish">
    <img src="https://cdn-icons-png.flaticon.com/512/2829/2829828.png" class="goldfish-bg" alt="Goldfish">

    <div id="root"></div>

    <script type="text/babel">
        const { useState, useEffect } = React;
        
        // Simple Icons component wrapper since we are using CDN
        const Icon = ({ name, size = 24, className }) => {
            useEffect(() => {
                if (window.lucide) {
                    window.lucide.createIcons();
                }
            }, [name]);
            return <i data-lucide={name} width={size} height={size} className={className}></i>;
        };

        const App = () => {
            const [drawResult, setDrawResult] = useState(null);
            const [isSpinning, setIsSpinning] = useState(false);
            const [showConfetti, setShowConfetti] = useState(false);

            const handleDraw = () => {
                if (isSpinning) return;
                setIsSpinning(true);
                setDrawResult(null);
                setShowConfetti(false);

                // Simulate spinning effect
                let counter = 0;
                const interval = setInterval(() => {
                    const temp = Math.floor(Math.random() * 3) + 7; // 7, 8, or 9
                    setDrawResult(temp);
                    counter++;
                    if (counter > 15) {
                        clearInterval(interval);
                        finishDraw();
                    }
                }, 100);
            };

            const finishDraw = () => {
                // Final result logic (Weighted slightly towards 8 and 9 for realism, but 7 is possible!)
                const rand = Math.random();
                let finalNum;
                if (rand < 0.2) finalNum = 7; // 20% chance for 7
                else if (rand < 0.6) finalNum = 8; // 40% chance for 8
                else finalNum = 9; // 40% chance for 9
                
                setDrawResult(finalNum);
                setIsSpinning(false);
                if (finalNum === 7) setShowConfetti(true);
            };

            const getDiscountText = (num) => {
                if (num === 7) return "驚喜 7 折！";
                if (num === 8) return "超值 8 折！";
                if (num === 9) return "開心 9 折！";
                return "";
            };

            const getMessage = (num) => {
                if (num === 7) return "手氣太旺了！老闆哭暈在廁所！";
                if (num === 8) return "恭喜！這是一個非常棒的折扣！";
                if (num === 9) return "不錯喔！買多一點最划算！";
                return "";
            };

            return (
                <div className="relative z-10 container mx-auto px-4 py-8 pb-20 max-w-2xl">
                    
                    {/* Header */}
                    <header className="text-center mb-12 animate-fade-in-down">
                        <div className="inline-block bg-pink-600 text-white px-4 py-1 rounded-full text-sm font-bold tracking-widest mb-4 transform -rotate-2">
                            BLACK FRIDAY SALE
                        </div>
                        <h1 className="text-6xl md:text-8xl font-black mb-2 neon-text text-transparent bg-clip-text bg-gradient-to-r from-purple-400 via-pink-500 to-red-500" style={{WebkitTextStroke: '2px white'}}>
                            黑五狂歡
                        </h1>
                        <h2 className="text-3xl md:text-5xl font-bold text-yellow-400 mb-6 drop-shadow-lg">
                            金魚大清倉
                        </h2>
                        <p className="text-xl text-cyan-200">
                            帶條好運金魚回家，明年風生水起！
                        </p>
                    </header>

                    {/* The 789 Game Section */}
                    <div className="glass-panel rounded-3xl p-8 mb-12 text-center border-2 border-pink-500 shadow-[0_0_50px_rgba(236,72,153,0.3)] relative overflow-hidden">
                        <div className="absolute top-0 left-0 w-full h-2 bg-gradient-to-r from-yellow-400 via-red-500 to-purple-600"></div>
                        
                        <div className="flex justify-center items-center gap-2 mb-6">
                            <Icon name="ticket" className="text-yellow-400" />
                            <h3 className="text-2xl font-bold text-white">789 幸運大盤</h3>
                            <Icon name="ticket" className="text-yellow-400" />
                        </div>
                        
                        <p className="text-gray-300 mb-8 text-sm md:text-base">
                            抽到 <span className="text-yellow-400 font-bold text-lg">7</span> 即享 7折 · 
                            抽到 <span className="text-cyan-400 font-bold text-lg">8</span> 即享 8折 · 
                            抽到 <span className="text-pink-400 font-bold text-lg">9</span> 即享 9折
                        </p>

                        {/* Result Display */}
                        <div className="flex justify-center mb-8">
                            <div className={`w-40 h-40 md:w-56 md:h-56 bg-black rounded-full border-4 flex items-center justify-center relative ${drawResult === 7 ? 'border-yellow-400 shadow-[0_0_30px_#fbbf24]' : 'border-gray-600'}`}>
                                {drawResult ? (
                                    <div className="text-center">
                                        <span className={`block text-8xl md:text-9xl font-black ${drawResult === 7 ? 'text-yellow-400' : drawResult === 8 ? 'text-cyan-400' : 'text-pink-400'}`}>
                                            {drawResult}
                                        </span>
                                    </div>
                                ) : (
                                    <span className="text-6xl text-gray-700 animate-pulse">?</span>
                                )}
                                
                                {showConfetti && (
                                    <div className="absolute inset-0 flex items-center justify-center pointer-events-none">
                                        <div className="absolute w-full h-full animate-ping border-4 border-yellow-500 rounded-full opacity-20"></div>
                                    </div>
                                )}
                            </div>
                        </div>

                        {/* Status Text */}
                        <div className="h-16 mb-6">
                            {drawResult && !isSpinning && (
                                <div className="animate-bounce">
                                    <h4 className="text-2xl md:text-3xl font-bold text-white mb-1">
                                        {getDiscountText(drawResult)}
                                    </h4>
                                    <p className="text-sm text-yellow-300">
                                        {getMessage(drawResult)}
                                    </p>
                                </div>
                            )}
                        </div>

                        {/* Button */}
                        <button 
                            onClick={handleDraw}
                            disabled={isSpinning}
                            className={`
                                px-10 py-4 rounded-full text-2xl font-black tracking-wider transition-all transform
                                ${isSpinning 
                                    ? 'bg-gray-700 cursor-not-allowed scale-95' 
                                    : 'bg-gradient-to-r from-yellow-400 via-orange-500 to-red-500 hover:scale-105 hover:shadow-[0_0_30px_rgba(245,158,11,0.6)] text-black'
                                }
                            `}
                        >
                            {isSpinning ? '抽獎中...' : '立即抽折扣'}
                        </button>
                    </div>

                    {/* Visual Highlights */}
                    <div className="grid grid-cols-2 gap-4 mb-12">
                        <div className="glass-panel p-4 rounded-xl text-center hover:bg-white/10 transition-colors">
                            <div className="text-4xl mb-2">🐠</div>
                            <h4 className="font-bold text-lg text-pink-300">極品泰獅</h4>
                            <p className="text-xs text-gray-400">頭瘤飽滿 體態圓潤</p>
                        </div>
                        <div className="glass-panel p-4 rounded-xl text-center hover:bg-white/10 transition-colors">
                            <div className="text-4xl mb-2">🐡</div>
                            <h4 className="font-bold text-lg text-cyan-300">頂級蘭壽</h4>
                            <p className="text-xs text-gray-400">背脊平滑 游姿優雅</p>
                        </div>
                        <div className="glass-panel p-4 rounded-xl text-center hover:bg-white/10 transition-colors">
                            <div className="text-4xl mb-2">🐉</div>
                            <h4 className="font-bold text-lg text-purple-300">優質龍睛</h4>
                            <p className="text-xs text-gray-400">眼大有神 尾鰭飄逸</p>
                        </div>
                        <div className="glass-panel p-4 rounded-xl text-center hover:bg-white/10 transition-colors">
                            <div className="text-4xl mb-2">✨</div>
                            <h4 className="font-bold text-lg text-yellow-300">全場配件</h4>
                            <p className="text-xs text-gray-400">魚缸、飼料、過濾器</p>
                        </div>
                    </div>

                    {/* Footer / CTA */}
                    <footer className="text-center mt-12 border-t border-gray-800 pt-8">
                        <p className="text-gray-400 text-sm mb-4">
                            *活動條款：
                            1. 每個帳號/每人每天限抽一次。
                            2. 折扣僅限 Black Friday 期間使用。
                            3. 7折優惠不可與其他優惠疊加。
                        </p>
                        <div className="flex justify-center gap-4">
                            <button className="flex items-center gap-2 bg-gray-800 hover:bg-gray-700 px-4 py-2 rounded-lg transition-colors">
                                <Icon name="map-pin" size={18} /> 店舖位置
                            </button>
                            <button className="flex items-center gap-2 bg-green-700 hover:bg-green-600 px-4 py-2 rounded-lg transition-colors">
                                <Icon name="message-circle" size={18} /> 聯絡我們
                            </button>
                        </div>
                        <p className="mt-8 text-xs text-gray-600">© 2023 Black Friday Goldfish Sale. All Rights Reserved.</p>
                    </footer>

                </div>
            );
        };

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>
